# Manual Dotfiles

These files are plain backups of the former standalone Home Manager shell setup.

- `fish/config.fish` -> `~/.config/fish/config.fish`
- `starship/starship.toml` -> `~/.config/starship.toml`

Install the referenced tools with your system package manager or another toolchain.
